using System.Reflection;

namespace Lotus.Foundation.Kernel.Utils
{
    public class ReflectionUtils
    {
        public static PropertyInfo GetPropertyWithPath(string path, object value)
        {
            //todo: resolve property with path against value - crawl until last .
            return null;
        }

        public static FieldInfo GetFieldWithPath(string path, object value)
        {
            //todo: resolve property with path against value - crawl until last .
            return null;
        }
    }
}